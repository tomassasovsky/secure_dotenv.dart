name: example
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  dotenv_gen: ^0.0.2

dev_dependencies:
  build_runner: ^2.4.5
  dotenv_gen_runner:
    path: ../dotenv_gen_runner
  flutter_lints: ^2.0.1

dependency_overrides:
  dotenv_gen:
    path: ../dotenv_gen
  dotenv_gen_runner:
    path: ../dotenv_gen_runner

flutter:
